const categoryRows = document.querySelector('.category__rows');
const group = `
<a class="benefit__block" tabindex="0" href = '../../card.html'>
    <div class="benefit__image category__image">
    </div>
    <div class="benefit__price">
        <div class="benefit__discount" aria-label="Старая цена">
            140 590 ₽
        </div>
        <div class="benefit__full">109 090 ₽</div>
    </div>
    <div class="benefit__item">ASUS G513IE-HN004T</div>
</a>
`;

categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);

categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);

categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);
categoryRows.insertAdjacentHTML('beforeend', group);